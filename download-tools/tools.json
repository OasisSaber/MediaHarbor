{
  "schema_version": 1,
  "platforms_note": "Windows x64 is the only officially supported platform.",
  "tools": {
    "yt-dlp": {
      "roles": ["probe", "vod", "playlist", "subtitle", "metadata"],
      "required": true,
      "platforms": {
        "windows-x64": "yt-dlp/yt-dlp.exe"
      }
    },
    "ffmpeg": {
      "roles": ["merge", "convert"],
      "required": true,
      "platforms": {
        "windows-x64": "ffmpeg/ffmpeg.exe"
      }
    },
    "ffprobe": {
      "roles": ["validate"],
      "required": true,
      "platforms": {
        "windows-x64": "ffmpeg/ffprobe.exe"
      }
    },
    "yutto": {
      "roles": ["bilibili"],
      "required": false,
      "platforms": {
        "windows-x64": "yutto/yutto.exe"
      }
    },
    "streamlink": {
      "roles": ["live"],
      "required": false,
      "platforms": {
        "windows-x64": "streamlink/streamlink.exe"
      }
    },
    "n-m3u8dl-re": {
      "roles": ["hls", "dash", "mss"],
      "required": false,
      "platforms": {
        "windows-x64": "N_m3u8DL-RE/N_m3u8DL-RE.exe"
      }
    },
    "gallery-dl": {
      "roles": ["social", "gallery", "post-media"],
      "required": false,
      "platforms": {
        "windows-x64": "gallery-dl/gallery-dl.exe"
      }
    }
  }
}
